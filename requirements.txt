# HTTP/Web
httpx>=0.23.0
requests>=2.28.0

# WebSocket
websockets>=10.0

# Display/Hardware
pillow>=9.0.0
#luma.lcd>=3.0.0                  # ILI9488 display driver
#luma.core>=2.4.0                 # Display core library

# Adafruit CircuitPython Libraries (for GPIO, buttons, sensors)
adafruit-blinka>=8.0.0           # GPIO board support for CircuitPython
adafruit-circuitpython-pn532>=1.4.0   # NFC/RFID reader (I2C)

# GPIO and RFID
RPi.GPIO>=0.7.0                  # Raspberry Pi GPIO control
#pirc522>=0.0.2                   # RC522 RFID reader (SPI)

# Rotary Encoder
ruhrohrotaryio>=1.0.0            # Rotary encoder with CircuitPython

# GPIO (Raspberry Pi specific)
lgpio>=0.2.0                     # New RPi GPIO library (alternative to RPi.GPIO)

# Async
aiofiles>=23.0.0

# Configuration
python-dotenv>=0.21.0
pydantic>=2.0.0

# Logging
python-json-logger>=2.0.0
